{
  "private": true,
  "scripts": {
    "setup:test-env": "python -m venv backend/.venv && backend/.venv/bin/python -m pip install --upgrade pip && backend/.venv/bin/python -m pip install -r backend/requirements-dev.txt",
    "test:backend": "backend/.venv/bin/python -m pytest backend/tests.py",
    "test:local": "npm run test:backend && npm --prefix frontend run build && npm run test:web",
    "test:web": "playwright test",
    "test:web:headed": "playwright test --headed",
    "test:web:install": "playwright install chromium"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "pinia": "^3.0.4",
    "vue-router": "^5.0.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "adm-zip": "^0.5.16"
  }
}
