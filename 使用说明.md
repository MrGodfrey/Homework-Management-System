# 作业提交系统使用与开发说明

## 1. 用户使用说明

系统分为**教师端**和**学生端**两套完整的操作流程。

### 1.1 教师端 (管理员)
作为主导课程的教师，您可以通过该系统完成从学生名单初始化到收集下载作业的所有闭环操作。
1. **系统初始化与登录**：
   - 第一次使用前，由管理员在服务器端运行 `python backend/create_admin.py` 创建您的初始教师实名/密码账号。
   - 访问入口系统并选择“教师登录”，输入账号密码。
2. **学生管理**：
   - 登录后，进入**学生管理模块**，上传 `.csv` 格式的学生名单（包含学号、姓名）。
   - 在平台上一键为全部学生生成随机密码，平台会输出一份包含明文密码的下载文件（请妥善保存并分发给对应学生，系统库中仅存哈希值）。
   - 如果有个别学生忘记密码，您可以单独为其执行“重置密码”操作，获取新的初始密码。
3. **作业管理**：
   - 进入**作业管理**，点击“新建作业”，输入作业标题、描述，并设定具体的**截止时间**和**是否允许迟交**。
   - 您还可以限制上传文件的最大体积及允许的文件格式（如 `.pdf`, `.zip`）。
4. **收作业与数据看板**：
   - 进入**数据看板（Dashboard）**，可以查看以“学生 × 作业”形式呈现的矩阵表格，直观把握全班每个人的提交进度（未交、按时交、迟交）。
   - 进入**作业详情**，可选择一键将全部学生的作业打包下载（系统提供“全量版本下载”及“仅最新版本下载”两种模式）。

### 1.2 学生端
作为学生，您的主要任务是查看作业要求并提交符合规范的文件。
1. **登录系统**：
   - 访问系统首页选择“学生登录”。
   - 按照要求输入您的**学号**、**姓名**，并输入教师下发的初始密码进行登录（注：密码输错 5 次将被锁定 10 分钟）。
2. **查看作业**：
   - 进入系统即可看到**作业列表**，清晰展示出所有已布置的作业清单及本人的提交状态（未提交/已提交/迟交）。
   - 点击具体作业可以查看详细要求和时间节点。
3. **提交作业**：
   - 在该作业详情页内触发上传，支持进度条显示。
   - 若作业在截止时间之前或允许迟交，您均可重新上传新版本，系统支持无限制多次提交。
   - 每次提交会被分配一个独立的递增版本号（如 v1, v2），所有文件会安全保存至云端对象存储（COS）。
4. **我的历史提交记录**：
   - 可以在“提交历史”模块随时回溯每次的提交时间与版本。
   - 提供临时生成的安全下载链接，供您下载核对自己上传的文件是否准确。


---


## 2. 开发者说明

本项目由由前端 (Vue 3) 和 后端 (FastAPI) 组成，以下为目录结构及文件功能总览：

### 2.1 根目录文件
- **`plan.md` / `阶段 *.markdown` 等**：记录开发计划、各阶段的任务清单、部署与验收规范。
- **`student.csv` / `passwords.csv`**：测试与数据导入的范例名单以及生成的密码导出表格。
- **`package.json`**：可能包含外层脚本与工作区信息定义。

### 2.2 后端服务 `backend/`
后端采用 `FastAPI` + `SQLAlchemy` 开发，主要存放 API 业务逻辑：

- **`create_admin.py`**：命令行初始化脚本，用于第一次创建系统的教师管理账号。
- **`requirements.txt`**：Python 依赖包清单（fastapi, sqlalchemy, passlib, cos-python-sdk-v5 等）。
- **`tests.py`**：后端功能的单元/集成测试脚本。
- **`app/`** 目录：核心应用代码
  - **`main.py`**：FastAPI 应用主入口，负责配置中间件（CORS）与整体路由的挂载。
  - **`config.py`**：配置模块，从环境变量（`.env` 等）读取数据库连接、JWT 密钥以及腾讯云 COS 认证等凭证。
  - **`database.py`**：数据库配置，初始化 SQLite 引擎及 SQLAlchemy ORM 对应的 Session 会话。
  - **`models.py`**：数据表模型定义，包括教师、学生、作业、提交记录、提交文件与日志等 6 张表。
  - **`schemas.py`**：Pydantic 模型，用于接口的输入数据校验和输出数据系列化。
  - **`auth.py`**：核心身份认证工具库，包括密码的加盐哈希（bcrypt）与 JWT 的签名与解析。
  - **`dependencies.py`**：FastAPI 依赖项组，例如基于 JWT 鉴权的 `get_current_student` 与 `get_current_instructor`。
  - **`cos_utils.py`**：腾讯云对象存储（COS）操作封装模块，包括直传文件和生成预签名的安全下载链接。
  - **`routers/`**：各业务模块接口路由：
    - `auth.py`：负责处理学生与教师的登录以及 Token 分发。
    - `student.py`：负责学生查看列表、提交上传处理、查看历史等相关 API。
    - `admin.py`：负责教师的名单管理、密码生成、作业管理、打包下载及监控看板。

### 2.3 前端页面 `frontend/`
前端采用 `Vue 3` + `Vite` 构建，界面风格化依赖 `Element Plus`。

- **构建与入口配置**：
  - `package.json` / `vite.config.js`：定义构建和依赖（vue-router, pinia, axios 等）。
  - `src/main.js`：Vue 实例初始化。
  - `src/App.vue`：布局入口容器。
  - `src/style.css`：全局基础样式。
- **核心模块 `src/`**：
  - **`router/index.js`**：全局路由映射管理表，区分 `/student/*` 和 `/admin/*`，并配置了全局的 Token 权限守卫。
  - **`stores/auth.js`**：Pinia 状态管理模块，集中控制当前登录的用户信息、角色（学生/教师）和 Token。
  - **`utils/api.js`**：Axios http 客户端实例配置，负责拦截请求注入 Authorization JWT 头，拦截响应集中处理 401（重定向回到登录）。
- **教师端视图 `src/views/admin/`**：
  - `Login.vue`：教师密码登录面板。
  - `StudentManage.vue`：名单导入、密码列表下载面板。
  - `AssignmentManage.vue`：新建/修改作业的表单页。
  - `Dashboard.vue`：学生整个课程提交状态概览矩阵页。
  - `SubmissionDetail.vue`：收缴详情、历史查看及一键打包动作执行面板。
- **学生端视图 `src/views/student/`**：
  - `Login.vue`：学号+姓名+密码验证界面。
  - `AssignmentList.vue`：卡片式或列表式呈现作业要求与自身提交状态。
  - `AssignmentDetail.vue`：作业详情展示以及包含进度条逻辑的文件拖拽上传组件。
  - `SubmissionHistory.vue`：历史提交版本记录列表及下载文件锚点。

## 3. 本地测试

运行 `python start_local_test.py` 即可自动完成后端环境搭建、数据库初始化检查、前端环境搭建以及端口占用处理等一系列复杂流程，最终在终端输出前后端访问地址。

```bash
pkill -f uvicorn ; pkill -f vite ; pkill -f start_local_test.py
```