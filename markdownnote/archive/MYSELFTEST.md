# 网站回归测试说明

这套仓库现在有一条默认的网站回归命令：

```bash
npm run test:web
```

以后只要改动了网页相关代码，默认都跑这条命令。它是当前仓库的回归门禁。

后端接口和打包目录结构另有一组轻量测试：

```bash
PYTHONPATH=backend python -m pytest backend/tests.py
```

## 一次性准备

先保证下面三件事已经完成：

```bash
# 根目录
npm install
npm run test:web:install

# 后端依赖
backend/.venv/bin/pip install -r backend/requirements.txt
```

如果你没有 `backend/.venv`，先按现有项目方式创建它，再安装依赖。

## 这条测试会做什么

`npm run test:web` 会自动完成下面这些事情，不需要手动起前后端：

1. 启动一个隔离的后端测试实例。
2. 使用临时 SQLite 数据库，不碰你现有本地库，更不会碰生产库。
3. 使用本地临时文件存储代替真实 COS，不碰线上对象存储。
4. 启动一个隔离的前端 Vite 实例。
5. 用 Playwright 跑完整浏览器闭环。

测试运行时使用的端口是：

- 前端：`127.0.0.1:4173`
- 后端：`127.0.0.1:18000`

这些服务由 Playwright 自动拉起和回收。

## 当前覆盖范围

这套回归已经覆盖了网站主流程里最核心、最容易出问题的部分：

- 未登录访问的路由重定向
- 学生登录
- 学生查看作业列表
- 学生查看互动记录
- 学生查看作业详情
- 学生看到单次提交总大小上限提示
- 学生下载作业附件
- 学生非法文件类型提交被拒绝
- 学生合法多文件提交
- 学生查看版本历史
- 学生逾期提交被拒绝
- 教师登录
- 教师看板查看提交矩阵
- 教师新增互动记录
- 教师删除互动记录
- 教师导入学生 CSV
- 教师手动新增学生
- 教师编辑学生
- 教师重置单个学生密码
- 教师批量重置密码
- 教师下载密码 CSV
- 教师删除学生
- 教师新建作业
- 教师布置作业时看到学生提交大小上限提示
- 教师编辑作业
- 教师上传作业附件
- 教师下载作业附件
- 学生提交新建作业
- 教师删除已有提交的作业
- 教师查看提交详情
- 教师下载单个学生提交 ZIP
- 教师下载最新版 ZIP，且包含本次作业所有已提交学生的最新版文件
- 教师下载全部版本 ZIP，且包含本次作业所有已提交学生的所有版本文件
- 教师评分
- 教师导出单作业 CSV
- 教师导出全量成绩 CSV
- 教师导出全部作业 ZIP，目录为作业 / 学生 / 版本 / 文件
- 学生刷新后看到最新分数
- 学生退出登录
- 教师退出登录

## 使用要求

以后对网页做改动时，按下面的标准执行：

1. 改完代码先跑 `npm run test:web`
2. 只有这条命令通过，才算本地网页回归通过
3. 如果失败，先修到绿，再继续别的改动

## 结果解释

这条测试通过，说明当前网站的主要闭环、下载、提交流程、管理流程和评分导出流程都能跑通。

它已经足够作为这个仓库的默认网页回归测试，但它仍然不是“数学意义上的绝对无 bug 证明”。如果以后新增功能页面，也要把新增功能继续补进这条回归里，而不是另起一套分散流程。
