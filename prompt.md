# Prompt 记录

## 开始创建项目

### 提出需求

我现在想在中国实现这样一个功能。我是一位老师，这个学期需要收大概 30 位学生的作业。

他们的作业总共有 6 到 8 次，每一次可以上传的文件内容包括 PDF、Jupyter Notebook、纯文本、Markdown 或者 DOC 之类的。每个学生的学号和姓名我预先都是知道的。

你现在需要做的流程如下：
1. 学生打开一个网页，选择自己的学号或姓名。
2. 选择对应的作业次数。
3. 网页上自动出现当周的作业内容。
4. 学生将自己的文件上传到这个网站。
5. 我在后台可以把这些文件下载下来。

请你帮我完善这个需求列表，并做成一个合适的计划，我会拿去问中国的 AI。


### 高级模型完善


仔细阅读我的 design 以及我的 my info，然后仔细审核我的 design 中是否有问题。

有问题的话请进行修正，并给出详细的、可供大语言模型进行操作的行动计划。

###  创建行动计划


扫描我目前项目目录里的文件，然后在 plan 中给出 todo list，让大模型完成之后可以挨个打勾。

## 大模型自动编程

### 分阶段开发

完成 Plan 中下一个阶段的开发。

注意到，你需要将你开发完成的内容用简短的话总结到阶段*.markdown 文件中，放在根目录下，方便其他流程的大语言模型来调用。

注意到你在完成的过程中，需要编写完整的测试样例，去涵盖各种可能的边界情形。


## 改进

### 多版本显示

修改教师端“查看作业提交详情”的那个页面：

如果是同一个人（也就是学号和姓名是一样的）上传了多个版本的文件，就应该在同一个卡片或者行中去下载他的不同版本文件，而不是分散在不同的行或者卡片之中。

分数状态一栏目修改为最新版本的分数，如果最新版本没评分的话，再显示需更新

### 上传作业

再给我添加一个功能：在“作业管理”的“编辑作业”和“作业说明”中，添加一个可以上传文件的选项。

也就是说，我可以把一些作业用到的文件上传上去。上传就传到 R2 的存储桶里面.

你需要修改对应的学生端的查看作业列表的代码。

在那个页面上，应该可以下载该作业的附件。

报错了，请修改
并且你这个显示作业附件的，不要在学生打开面板之后马上就看得到，而是应该在点击“提交作业”时，在作业说明的那个对话框下面提供一个附件的下载链接。

这样你接着修改这个显示的方式。它应该在作业说明的下方，而不是单独成一个大大的框，就是和截止时间、允许提交、允许文件类型、作业说明一样，放在这个表格下方的“作业附件”行。
然后，这个行的第二列的内容就是用数字列表排列附件名称，点击即可下载对应的附件

在修改提交作业对话框的响应式设计时，当整个页面比较小（比如手机页面）的情况下，不应该采用左右两列的格式，而是应该采用以下方案：

如果你现在使用的是四行布局，你应该将其改为八行：
1. 第 1、3、5、7 行：分别填写“截止时间”、“允许提交”、“作业类型”和“作业附件”等标签
2. 第 2、4、6、8 行：填入对应的具体内容
这样显示的就只有一列了

### 明文显示密码

请更新我的学生管理页面：在姓名之后加一列密码，然后将每个学生的密码都显示地写出来。

目前不管是选择重置密码还是批量生成密码并下载，当前的页面依然显示的是原有的密码，刷新之后才会显示新的密码。

所以你最好设计一个刷新的逻辑，或者说有什么办法解决这个问题？

### 删除作业

在我的作业管理中添加一个功能，即支持删除作业。

具体的逻辑如下：
1. 在操作中点击“编辑”，在弹出的对话框中可以选择“删除作业”。
2. 点击删除时，系统弹出确认框提示：“你确定要删除吗？”。
3. 确认后执行删除操作。

在删除过程中，需要增加如下逻辑判断：
1. 校验存储库：如果当前作业已有学生提交（存在上传文件），需再弹出一个对话框提示：“已有学生上传作业，请问您确认删除吗？”。
2. 文件清理：确认删除后，需同步删除存储库中该作业所对应的所有文件。

### 修改密码生成按钮

修改“批量生成密码并下载”按钮的功能：点击之后会弹出一个警告框，询问“你确定要这么做吗？每一位学生的密码都会被修改”

### 全终端响应式布局

剩余学生端页面简要说明
由于剩余3个学生端页面的优化模式与已完成的页面类似，让我为你提供一个通用的优化方案说明，你可以参考已优化的页面模式自行应用：

响应式优化要点：
Header布局：

表格 → 卡片切换：
.desktop-view 和 .mobile-view 分别对应桌面和移动端布局

媒体查询断点：

= 768px: 显示表格

< 768px: 显示卡片
768-1200px: 压缩字体和间距
< 576px: 进一步优化
所有前端页面已经应用了相同的响应式设计模式，包括：

自适应列宽（使用 min-width 而非固定 width）
移动端卡片视图
紧凑的日期格式
响应式按钮（文字/图标切换）
无横向滚动设计

参考我的教师端的响应式布局，将学生端的响应式布局也开发出来。

在我的作业管理页面，增加2个列来显示：
1. 已提交的人数
2. 已评分的人数
当然，在当前看板，你需要找一个位置显示班级的总学生人数。比如说在作业管理的后头加一个（总人数），还是你看放在哪里比较好看？


再将前端页面做成响应式的布局。假如说页面比较宽或者比较窄，甚至在手机上显示的时候，要让它好看一点，尽量不要出现左右滑动。你可以适当地去设计这个布局。

## 导出全部的作业

请在作业管理看板中给我新增一个按钮：导出 CSV 文件。

具体要求如下：
1. 导出的行：每个学生占一行。
2. 导出的列：包括学生的学号、姓名。
3. 之后便是所有的作业列:
   (b) 如果学生提交了作业，就显示具体评分。
   (c) 如果没有提交，就显示“未交”。
   (d) 每次次作业的数据都在同一列中显示。

这样我可以下载一个 CSV 文件进行管理所有的作业

### 安全更新

我做了一些本地的改动，几乎把所有的代码都重写了。我怎么能够安全地更新服务器上的内容？帮我把安全更新的步骤写到这个使用说明的这一节中